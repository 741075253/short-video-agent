# 短剧视频生成

将小说文本转换为竖屏动画短剧的生成系统。

## 语言

**项目（Project）**:
一个创作单元，包含源文本、风格设定、分镜数据和生成产物。
_避免使用_: 文档, 任务

**镜头（Shot）**:
视频的最小组成单元。一个镜头 = 一张关键帧图片 + 一段视频片段 + 字幕 + 旁白，时长 5-10 秒。
_避免使用_: 帧, 剪辑, 场景

**场景（Scene）**:
多个连续镜头的逻辑分组，共享同一地点和角色配置。

**分镜剧本（StoryPackage）**:
文本解析后的结构化产物，包含角色设定、场景描述、镜头序列。
_避免使用_: 脚本, 剧本

**角色（Character）**:
从文本中抽取的叙事主体，包含姓名、外观描述、性格描述。

**生图（Image Generation）**:
将镜头描述文本转换为静态关键帧图片的过程，使用 AI 图片生成模型。
_避免使用_: 渲染, 画图

**图生视频（Image-to-Video / I2V）**:
将静态关键帧图片 + 动作描述转换为短视频片段的过程，使用 AI 视频生成模型。
_避免使用_: 动画化, 视频渲染

**视频生成服务层（Video Generation Service Layer）**:
抽象的视频生成接口，支持本地模型、第三方 API、混合模式等多种后端实现。
_避免使用_: 转码层

**成片（Final Output）**:
所有镜头视频片段经 FFmpeg 拼接、叠加字幕后产出的完整 MP4 文件。
_避免使用_: 最终视频, 成品
